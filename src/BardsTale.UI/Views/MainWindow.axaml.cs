using Avalonia.Controls;

namespace BardsTale.UI.Views;

/// <summary>The desktop top-level window; all content lives in the shared <see cref="MainView"/>.</summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
