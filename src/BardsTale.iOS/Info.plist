<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>The Bard's Tale</string>
	<key>XSAppIconAssets</key>
	<string>Assets.xcassets/AppIcon.appiconset</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Take the whole screen on iPad (opt out of Split View / Slide Over) so the app fills
	     the display and strictly honors the landscape lock at launch instead of letterboxing. -->
	<key>UIRequiresFullScreen</key>
	<true/>
	<!-- A real launch storyboard (dark theme, centered title) gives iOS the full native
	     resolution and a correctly oriented landscape launch with no white flash. -->
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIDeviceFamily</key>
	<array>
		<integer>1</integer>
		<integer>2</integer>
	</array>
	<!-- The UI is laid out for a wide screen, so lock to landscape (iPad + iPhone). -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
